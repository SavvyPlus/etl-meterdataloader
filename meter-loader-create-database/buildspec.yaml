version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r meter-loader-create-database/requirements.txt -t meter-loader-create-database

artifacts:
  base-directory: meter-loader-create-database
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
